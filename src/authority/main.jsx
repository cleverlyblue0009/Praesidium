import React from 'react';
import ReactDOM from 'react-dom/client';
import AuthorityEntry from './AuthorityEntry';
import './authority.css';

ReactDOM.createRoot(document.getElementById('authority-root')).render(
  <React.StrictMode>
    <AuthorityEntry />
  </React.StrictMode>
);
